import { defineConfig } from 'dumi';
import { homepage } from './package.json';

export default defineConfig({
  themeConfig: {
    name: '@ant-design/theme-algorithms',
    github: homepage,
  },
});
